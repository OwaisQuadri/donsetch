# cargo-nextest config: https://nexte.st/docs/configuration/
#
# The cargo profile is pinned on the command line
# (`--cargo-profile ci`) in the Justfile and CI: some installed
# nextest versions ignore the config-level cargo-profile key.
#
# Local: `cargo nextest run`           → fail-fast, stop at first red
# CI:    `cargo nextest run --profile ci` → run everything, show all

[profile.default]
fail-fast = true

[profile.ci]
fail-fast = false